# This is not used, but is here for reference.
apiVersion: scheduling.kcp.dev/v1alpha1
kind: Location
metadata:
  annotations:
    kcp.dev/cluster: {{.Workspace}}
    name: {{.Name}}
spec:
  instanceSelector: {}
  resource:
    group: workload.kcp.dev
    resource: synctargets
    version: v1alpha1
