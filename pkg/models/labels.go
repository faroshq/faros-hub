package models

const (
	// LabelUser is the label used to identify the user
	LabelUser = "faros.sh/user"
	// LabelOwner is the label used to identify the owner
	LabelOwner = "faros.sh/owner"
	// LabelName is the label used to identify the name
	LabelName = "faros.sh/name"
	// LabelIdentifier is the label used to identify the object
	LabelIdentifier = "faros.sh/identifier"
)
